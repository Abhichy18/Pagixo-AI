# Pagixo OCR API — FastAPI Bridge Dependencies
# These are SEPARATE from the Streamlit app requirements.
# Install with: pip install -r api/requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0
python-dotenv>=1.0.0

# These are also required by the OCR pipeline (likely already installed from root):
# pillow
# openai
# requests
# PyMuPDF
# opencv-python
# numpy

# ─── Testing ────────────
pytest>=8.0.0
httpx>=0.27.0
anyio>=4.0.0
pytest-anyio>=0.0.0
